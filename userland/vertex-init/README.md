# vertex-init

Placeholder for the first user-space process in Krust-native mode.

For the hosted Linux prototype, `crates/vertex-supervisor` currently represents the root service named `svc:vertex-supervisor`.
