# vertex-init

Placeholder for the first user-space process in Krust-native mode.

For host-side graph simulation, `crates/vertex-supervisor` represents the root
service named `svc:vertex-supervisor`. Krust-native boot uses a standalone
`vertex-init` process instead.
